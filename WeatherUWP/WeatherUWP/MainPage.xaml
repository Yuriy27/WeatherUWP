<Page
    x:Class="WeatherUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WeatherUWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding MainPageVmInstance, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Button x:Name="forecast" Width="100" Height="40" Command="{Binding GoToForecast}" Content="Forecast" HorizontalAlignment="Left"></Button>
        <Button x:Name="settings" Width="100" Height="40" Command="{Binding GoToSettings}" Content="Settings" HorizontalAlignment="Center"></Button>
        <Button x:Name="history" Width="100" Height="40" Command="{Binding GoToHistory}" Content="History" HorizontalAlignment="Right"></Button>
    </Grid>
</Page>
